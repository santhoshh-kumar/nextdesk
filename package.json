{
  "name": "nextdocs-root",
  "private": true,
  "packageManager": "npm@11.6.2",
  "engines": {
    "node": ">=24.14.1",
    "npm": ">=11.6.2"
  },
  "workspaces": [
    "web",
    "realtime"
  ],
  "scripts": {
    "check:lockfiles": "node scripts/check-workspace-lockfiles.js",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "turbo run format",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6",
    "turbo": "^2"
  }
}
